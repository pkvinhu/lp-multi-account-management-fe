import { ThunkAction } from 'redux-thunk';
import { RootState } from '..';
import { UserPayload, User, UsersError, UserAction, SET_LOADING, GET_USERS, SET_ERROR } from './types';

export const getUsers = () => {
    
}